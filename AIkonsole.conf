# Enable or disable logging (yes/no)
LOGGING="yes"

# Path to the log file (if logging is enabled)
LOG_PATH="/home/$(whoami)/.logs/chat_history.log"


# Timeout for API request (in seconds)
API_TIMEOUT=10

# Output format: plain (just the answer) or detailed (with API response metadata)
OUTPUT_FORMAT="plain"

# Default language for the prompt (e.g., en, es, fr)
DEFAULT_LANGUAGE="en"



# Number of retries for API requests
RETRY_COUNT=3

# Delay between retries (in seconds)
RETRY_DELAY=5
